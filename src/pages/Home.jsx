import React from 'react';
import { useNavigate } from 'react-router-dom';
import List from '../components/List'

const Home = () => {
    return (
        <main>
            <List />
        </main>
    );
};

export default Home;